{
  "mcpServers": {
    "context7": {
      "command": "context7-mcp",
      "args": [
        "--transport",
        "stdio"
      ],
      "env": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      }
    }
  }
}
